<%= form_for(driverslist) do |f| %>
  <% if driverslist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(driverslist.errors.count, "error") %> prohibited this driverslist from being saved:</h2>

      <ul>
      <% driverslist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
